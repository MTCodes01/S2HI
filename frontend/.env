# Environment Configuration for Frontend

# API Base URL - Update this based on your deployment environment
# For local development with Docker: /api (uses same domain)
# For Docker: http://localhost:3001 (direct container access)
# For production: /api (uses reverse proxy on same domain)
VITE_API_BASE_URL=/api
